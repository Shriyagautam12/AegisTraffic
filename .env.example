# Gemini API key — required for AI Copilot (optional; system works offline without it)
GEMINI_API_KEY=your_gemini_api_key_here

# Only needed on Render (persistent disk mount path).
# Leave blank for local development — defaults to data/learning_log.db
DB_PATH=
